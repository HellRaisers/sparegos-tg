# ── Telegram (обязательно) ─────────────────────────────────
# Токен бота от @BotFather
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
# ID чата/группы/канала, куда слать сообщения (можно узнать через @userinfobot)
TELEGRAM_CHAT_ID=-1001234567890

# ── Необязательно ──────────────────────────────────────────
# Всё ниже имеет разумные значения по умолчанию в коде (config.py).
# Раскомментируй и поменяй только если нужно переопределить.
#
# Фильтр писем (синтаксис поиска Gmail). По умолчанию ловит сообщения
# клиентов (оба типа) и приглашения «Invitation to Apply».
# ВАЖНО: если задать здесь — правки фильтра через git push НЕ долетят на
# сервер, пока не обновишь .env. Лучше оставить закомментированным.
# GMAIL_SUBJECT_QUERY=subject:("sent you a message" OR "direct message from a client" OR "Invitation to Apply")
#
# Маршрутизация по клиентам: Имя=Тег[:chat_id];Имя=Тег[:chat_id]
# «Имя» ищется в отправителе письма (без учёта регистра). Тег становится
# хештегом в уведомлении (#HillTribe — видно, что это клиентское сообщение
# и по какому проекту). Если добавить :chat_id — копия уйдёт в рабочий чат
# проекта (бот должен быть добавлен в этот чат).
# CLIENT_ROUTES=Peng=HillTribe:-1001111111111;Liad=Truedose:-1002222222222
#
# Темы (форум) в основном чате. Работает, только если основной чат —
# супергруппа с включёнными темами. Значение — id темы (message_thread_id).
# Если не задано, всё падает в общую ленту чата, как раньше.
#   TOPIC_CLIENTS  — письма клиентов из CLIENT_ROUTES (Peng, Liad)
#   TOPIC_INVITES  — приглашения (Interview и Apply)
#   TOPIC_MESSAGES — остальные сообщения клиентов
# TOPIC_CLIENTS=2
# TOPIC_INVITES=3
# TOPIC_MESSAGES=4
#
# GMAIL_SENDER=upwork.com
# GMAIL_LOOKBACK=2d
# POLL_INTERVAL_SECONDS=600
